\cvbl{
\cvline{Linux}{Bugfix, driver and OpenRISC architecture support(merged into mainline)}
}{
\cvline{Linux}{Bug修复，驱动程序和OpenRISC架构支持(并入主干版本)}
}
\cvline{U-Boot}{OpenRISC architecture support(merged into mainline)}
\cvbl{
\cvline{OpenRISC}{Bugfix, system integration, platform migration(merged into mainline)}
}{
\cvline{OpenRISC}{Bug修复，系统整合和平台移植(并入主干版本)}
}
